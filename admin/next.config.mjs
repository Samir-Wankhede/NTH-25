/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/superusers-admin", // 👈 Set the base path
    assetPrefix: "/superusers-admin", // 👈 Ensures assets load correctly
};

export default nextConfig;
